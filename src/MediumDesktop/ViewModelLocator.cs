using MediumDesktop.Core.Managers.Interfaces;
using MediumDesktop.Core.ViewModels;

namespace MediumDesktop
{
    public class ViewModelLocator
    {
        //private readonly IContainer _container;

        //public ViewModelLocator(IContainer container)
        //{
        //    _container = container;
        //}

        //public LoginViewModel LoginViewModel => new LoginViewModel(_container.Resolve<ILoginManager>());

    }
}
