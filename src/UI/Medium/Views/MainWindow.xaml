<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:Medium.Views"
        Title="Medium">

  <Panel>
    <Carousel SelectedIndex="{Binding CurrentPageIndex}">

      <views:LoginView DataContext="{Binding AuthenticationViewModel}"/>

      <views:MainPageView/>

    </Carousel>
  </Panel>
</Window>